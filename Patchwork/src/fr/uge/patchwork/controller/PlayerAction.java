package fr.uge.patchwork.controller;

/*
 * Possible actions useful for the controller 
 * to logically set up the game scenarios
 */
enum PlayerAction {
  ADVANCE,
  SELECT_PATCH,
  BACK,
  QUIT,
  DEFAULT,
  PLACE,
}
